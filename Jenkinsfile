
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "username = ${u}"
                echo "username = ${adminPassword}"
            }
        }
    }
}
