
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "username = ${adminUser}"
                echo "username = ${adminPassword}"
            }
        }
    }
}
