
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "username = ${adminUsername}"
                echo "username = ${adminPassword}"
            }
        }
    }
}
