version: 2.1
jobs:
  build:
    docker:
      - image: "cimg/base:2021.11"
    steps:
      - run:
          command: echo "Building the project"
  test:
    docker:
      - image: "cimg/base:2021.11"
    steps:
      - run:
          command: echo "Testing the project"
  run:
    docker:
      - image: "cimg/base:2021.11"
    steps:
      - run:
          command: node main
workflows:
  version: 2
  build-and-test:
    jobs:
      - build
      - test:
          requires:
            - build
      - run:
          requires:
            - test
